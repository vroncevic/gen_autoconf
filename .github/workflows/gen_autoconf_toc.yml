name: gen_autoconf toc generator
on:
  push:
    branches: [ master ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: gen_autoconf toc generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2
